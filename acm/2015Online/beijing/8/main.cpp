#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    int T;
    double x;
    cin>>T;
    for (int t = 0; t < T; ++t) {
        cin>>x;
        if (8 == 1/x) {
            cout<<"test"<<endl;
        }
    }
    return 0;
}

